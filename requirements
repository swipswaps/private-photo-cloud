# All dependencies must be frozen

Django==1.11a1              # web framework, last stable version

six==1.10.0                 # core for py2+3 <- Django
pytz==2016.10               # timezones for Python <- Django
tzlocal==1.3.0              # get local timezone

dj-database-url==0.4.2      # database config via URL
django-cache-url==1.3.1     # cache config via URL
dj-email-url==0.0.10        # email config via URL

djangorestframework==3.5.3  # REST framework
Markdown==2.6.7             # for REST framework -- browsable API
#django-filter==1.0.1        # for REST framework -- filtering support
https://github.com/carltongibson/django-filter/archive/develop.tar.gz
#django-crispy-forms==1.6.1  # for REST framework -- filter forms support
# Use latest from dev till they fix warnings on Django 1.11
https://github.com/django-crispy-forms/django-crispy-forms/archive/dev.tar.gz

psycopg2==2.6.2             # PostgreSQL connector
django-redis==4.7.0         # django redis cache backend
redis==2.10.5               # redis connector

Pillow==4.0.0               # image processing library

# Enable upstream once fixed on Alpine
# python-magic==0.4.13        # recognize content type by content
https://github.com/ahupp/python-magic/archive/master.tar.gz

celery==4.0.2               # background worker
billiard==3.5.0.2           # <- celery
redis==2.10.5               # <- celery
kombu==4.0.2                # <- celery
amqp==2.1.4                 # <- celery
vine==1.1.3                 # <- celery

# DEV #
# Disable for instance -- it does not track changes in all files inside Docker for Maс
# pyinotify==0.9.6            # better check code updates using inotify

channels==1.0.2             # WebSockets for Django + sort of event-driven
daphne==1.0.1               # <- channels
asgiref==1.0.0              # <- channels
autobahn==0.17.1            # daphne <- channels
Twisted==16.6.0             # daphne <- channels. Use stable version here to prevent WS close
txaio==2.6.0                # autobahn <- daphne <- channels
zope.interface==4.3.3       # twisted <- daphne <- channels
constantly==15.1.0          # twisted <- daphne <- channels
incremental==16.10.1        # twisted <- daphne <- channels

asgi_redis==1.0.0           # needed for daphne
msgpack-python==0.4.8       # <- asgi_redis
